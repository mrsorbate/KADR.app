services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      PORT: ${BACKEND_PORT:-3000}
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      DATABASE_PATH: ${DATABASE_PATH:-/app/data/database.sqlite}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:8080}
      FUSSBALL_API_BASE_URL: ${FUSSBALL_API_BASE_URL:-https://api-fussball.de/api}
    volumes:
      - ${BACKEND_DATA_DIR:-./data/backend}:/app/data
      - ${BACKEND_UPLOADS_DIR:-./data/uploads}:/app/uploads
    ports:
      - "${BACKEND_PORT:-3000}:3000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      - backend
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped

